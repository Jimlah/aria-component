"use client";

import { SubmenuTrigger as AriaSubmenuTrigger, SubmenuTriggerProps } from "react-aria-components";

const SubmenuTrigger = (props: SubmenuTriggerProps) => {
  return (
    <AriaSubmenuTrigger
      {...props}
    />
  );
};

export default SubmenuTrigger;