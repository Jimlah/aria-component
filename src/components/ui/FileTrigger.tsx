"use client";

import { FileTrigger as AriaFileTrigger, FileTriggerProps } from "react-aria-components";

const FileTrigger = (props: FileTriggerProps) => {
  return (
    <AriaFileTrigger
      {...props}
    />
  );
};

export default FileTrigger;